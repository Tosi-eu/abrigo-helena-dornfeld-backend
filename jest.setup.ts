// Configuração inicial para todos os testes
// Este arquivo é executado antes de cada arquivo de teste

// Define NODE_ENV como 'test' para evitar tentativas de conexão real ao banco
process.env.NODE_ENV = 'test';

